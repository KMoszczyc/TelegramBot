services:
  bot:
    container_name: telegram-ozjasz-light-bot
    restart: always
    build:
      context: .
      dockerfile: docker/Dockerfile_bot
    env_file:
      - .env
    image: telegram-ozjasz-light-bot
    volumes:
      - ./data:/data
      - .:/app